# 系统垃圾文件
Thumbs.db
desktop.ini
.DS_Store

# 编辑器
.vscode/
.idea/
*.swp

# 直播间状态：本地计划任务每 2 分钟就要重写一次，不进仓库。
# 线上由网站自带的 Cloudflare 函数（functions/api/live.js）实时查询，见 README 说明。
# 不排除的话，自动上线脚本会每 2 分钟产生一次提交，把 Cloudflare 每月 500 次的构建额度用光。
data/live.js
